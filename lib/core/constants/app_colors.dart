import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1E6091);       // Cool deep blue-teal (new primary)
  static const Color secondary = Color(0xFF52B69A);     // Soft green-blue tone
  static const Color background = Color(0xFFF8F9FA);     // Clean light background
  static const Color card = Color(0xFFFFFFFF);           // Still white for clarity
  static const Color accent = Color(0xFFADC4CE);         // Pale steel blue for accents
  static const Color textPrimary = Color(0xFF1C1C1C);    // Almost black text for readability
  static const Color textSecondary = Color(0xFF495057);  // Dark gray-blue for subtitles
  static const Color error = Color(0xFFDC3545);          // Bright but elegant red for errors
}
